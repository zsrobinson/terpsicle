node_modules
dist
.output
.wrangler
.tanstack
.dev.vars*
*.local
coverage
playwright-report
test-results
.DS_Store

# Local ingest output (pnpm tsx scripts/ingest.ts … --target fs)
.data

# Mobile lab runs (scripts/mobile-lab); CI publishes them to the mobile-runs branch
mobile-lab-results
