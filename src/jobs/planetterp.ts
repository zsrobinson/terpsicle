import { type Job, notImplementedYet } from "./job";

/** PlanetTerp ratings, review metadata and grade distributions. */
export const runPlanetTerpJob: Job = async ({ now }) => {
  await notImplementedYet("planetterp", now);
};
