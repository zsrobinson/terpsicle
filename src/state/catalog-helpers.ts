import {
  creditsLabel as coreCreditsLabel,
  planCredits as corePlanCredits,
} from "~/core/plans";

/** @deprecated moved to ~/core/plans */
export const planCredits = corePlanCredits;

/** @deprecated moved to ~/core/plans */
export const creditsLabel = coreCreditsLabel;
